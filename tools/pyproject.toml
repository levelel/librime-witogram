[tool.poetry]
name = "witogram-tools"
version = "0.1.0"
description = "Minimal Python environment for witogram local probe tools"
authors = ["Outwit <noreply@example.com>"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.14,<3.15"
pypinyin = "^0.55.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
